<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CALCULADORA_PRO" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>

    <style name="botones_numericos">
        <item name="android:background">@color/black</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/_tamanioTeclado</item>
    </style>

    <style name="botones_operaciones">
        <item name="android:background">@color/black</item>
        <item name="android:textColor">#FF4D16</item>
        <item name="android:textSize">@dimen/_tamanioTeclado</item>
    </style>

    <style name="salida_operaciones">
        <item name="android:background">@color/black</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/_tamanioResultado</item>
    </style>

    <style name="historial">
        <item name="android:background">@color/black</item>
        <item name="android:textColor">@color/grey</item>
        <item name="android:textSize">@dimen/_tamanioResultado</item>
    </style>

</resources>